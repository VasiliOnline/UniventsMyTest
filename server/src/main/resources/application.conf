ktor = {
  deployment {
    port = 8080
    host = "0.0.0.0"
  }

  application {
    modules = [ com.example.univents.server.ApplicationKt.main ]
  }
}

database {
  url = "jdbc:postgresql://localhost:5432/univents"
  driver = "org.postgresql.Driver"
  user = "univents_user"
  password = "Univents123!"
}
